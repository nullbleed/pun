post_install() {
	# install instructions
    echo "To run pun automatically once every hour use the systemd-timer:"
    echo "    $ sudo systemctl start pun.timer"
    echo "    $ sudo systemctl enable pun.timer"
    echo ""
}

